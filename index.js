const createShopButton = document.querySelector('.button--add')class Popup {    constructor(selector) {        this.$el = document.querySelector(selector)        this.$el.addEventListener('mousedown', this.clickHandler.bind(this))    }    clickHandler(e) {        const type = e.target.dataset.type        if (type === 'cancel-button' || type === 'background' || type === 'reset-button') {            this.hide()        }    }    show() {        this.$el.classList.add('active')    }    hide() {        this.$el.classList.remove('active')    }    toggle() {        if (this.$el.classList.contains('active')) {            this.hide()        } else this.show()    }}const popup = new Popup('.popup')createShopButton.addEventListener('click', popup.show.bind(popup))